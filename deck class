class Deck():
    def __init__(self):
        self.deck=[]
        for suit in Suits:
            for rank in Ranks:
                created_card=Card(suit,rank)
                self.deck.append(created_card)
    def __str__(self):
        deck_comp=''
        for created_card in self .deck:
            deck_comp="\n"+ card.__str__()
        return 'The deck has '+deck_comp
    def shuffle(self):
        random.shuffle(self.deck)
    def deal_one(self):
        return self.deck.pop()
    
        
